fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0
PyYAML==6.0.2
python-dotenv==1.0.1

# LiveKit API (for room management + agent dispatch)
livekit-api>=0.8.0

# LangGraph + LangChain (kept for text fallback API)
langgraph==0.2.0
langchain==0.3.0
langchain-core==0.3.0
langchain-openai==0.2.0

# Audio processing
numpy<2.0

# Redis (optional, for production session store)
redis[hiredis]==5.1.0
